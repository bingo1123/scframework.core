<Window x:Class="YS.Yuanji.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YS.Yuanji.WPF"
        mc:Ignorable="d"
        Title="数据监控" Height="600" Width="1000">
       <Window.DataContext>
        <local:MainWindowViewModel/>
        </Window.DataContext>
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 机台信息和连接状态区域 -->
        <Border Grid.Row="0" Background="LightBlue" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="机台号:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="MachineNumberComboBox" 
                          Width="120" 
                          Height="30"
                          ItemsSource="{Binding MachineNumbers}"
                          SelectedItem="{Binding SelectedMachineNumber}"
                          VerticalContentAlignment="Center"/>

                <TextBlock Text="连接状态:" VerticalAlignment="Center" Margin="20,0,5,0"/>
                <Ellipse Width="20" Height="20" Margin="5,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="Red"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="未连接"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Text" Value="已连接"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 搜索过滤区域 -->
        <Border Grid.Row="1" Background="LightGray" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="SearchTextBox" 
                         Width="200" 
                         Height="30"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"/>
                <Button Content="清除" 
                        Command="{Binding ClearSearchCommand}"
                        Margin="10,0,10,0" 
                        Width="80"/>
                <Button Content="导出Excel" 
                        Command="{Binding ExportToExcelCommand}"
                        Margin="0,0,10,0" 
                        Width="100"/>
                <Button Content="导出TXT" 
                        Command="{Binding ExportToTxtCommand}"
                        Margin="0,0,0,0" 
                        Width="100"/>
            </StackPanel>
        </Border>

        <!-- 数据展示区域 -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding FilteredDataView}" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="数值" Binding="{Binding Value}" Width="150"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="更新时间" Binding="{Binding UpdateTime}" Width="200"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>